export class Transfer { }

describe('transfer', () => {
  const from = '';
  const to = '';
  const amount = '';
  const token = '';
  const recipient = '';

  test('new', () => {
    expect(new Transfer()).toBeTruthy();
  });
});
